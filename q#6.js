// Stripping Names: Store a person’s name, and include some whitespace characters at the beginning and end
//  of the name. Make sure you use each character combination, , at least once. Print the name
//   once, so the whitespace around the name is displayed. Then print the name after striping the
//    white spaces.
let personname1 = " \t   sameed ahmed   \n";
console.log("Name With whitespace:", personname1);
let strippedname = personname1.trim();
console.log("strippedname:", strippedname);
// dusra tareeqa
let Name = " \t   sameed ahmed   \n";
console.log("|" + Name + "|");
console.log("|" + Name.trim() + "|");
